---
test:

    load:
        begin_step: 10
        end_step: 80
        steps: 10

    data: 
        image: "/userdata/Rt.zip/Rt-Gene"
        label: "/userdata/Rt.zip/Rt-Gene/RT-label/Label-glasses/train" 
        name: rt
        header: True
        isFolder: True

    savename: "evaluation"

    device: 0

    reader: reader

# dropout = 0
# dim_feed = 512
