---
test:

    load:
        begin_step: 10
        end_step: 80
        steps: 10

    data: 
        image: "/userdata/MPII.zip/MPIIFaceGaze/Image"
        label: "/userdata/MPII.zip/MPIIFaceGaze/Label"
        name: mpii
        header: True
        isFolder: True

    savename: "evaluation"

    device: 0

    reader: reader

# dropout = 0
# dim_feed = 512
